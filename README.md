# DALSolutions ESLint config

## Setup

Install dependencies:
```
npm i -D eslint @dalsolutions/eslint-config
```

### Next

Inside `eslint.config.mjs`
```
import config from '@dalsolutions/eslint-config/next.mjs'

export default config
```
